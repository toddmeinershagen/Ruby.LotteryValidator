require 'rspec/core/rake_task'

task :default => [:run] do
end

task :run do
  puts 'You have run successfully.'
end

RSpec::Core::RakeTask.new(:spec)

task :test => [:spec] do
end

task :initialize do
  # @sh = CommandShell.new
end
